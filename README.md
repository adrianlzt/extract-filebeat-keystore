# Extract contents of filebeat keystore file
Put the ``filebeat.keystore`` file in this same folder. Then build and run:
```
$ go run .
foo: bar
```

The keystore file it is normally in /var/lib/filebeat/filebeat.keystore
